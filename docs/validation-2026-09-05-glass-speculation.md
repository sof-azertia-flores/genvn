# 液态玻璃、预备形象与二轮预推演验证

2026-09-05。在独立临时目录构建与测试，没有调用付费模型或写入真实存档。

- 后端 179 项测试全部通过，离线 Gradle `test bootJar` 成功。
- 前端 52 项测试、TypeScript 与 Vite 生产构建通过。
- 第二轮专项验证：第一轮完成门槛、概率乘积前四名、检定双结果上限、分支状态/地点/对话历史隔离、选中父路线后缓存复用、过期和删除取消。
- 形象池专项验证：只有外貌资料、采用后保持图片字节和生成次数不变、正式剧情提交后才绑定 NPC/Bible、四个图片请求实际同时运行、闲时预算保留、重试预算和参考图依赖。
- 使用本地文本 mock 与图片 HTTP 桩进行浏览器验收：1440×1000 和 390×844，开局日志/进度、主舞台、角色卡、两轮队列、历史弹窗；手机页面没有横向溢出。
- 独立 HTTP 游戏流程连续选择两次，两个响应均 `fromSpeculativeCache=true`，下一轮仍只有四个第二层候选。
- 后台队列显示估计概率和任务状态，不显示候选场景正文。部分失败不会报告全部就绪。

验证限制：没有重新验证真实提供商的画质、延迟、限流或真实玩家偏好预测准确率；概率用于安排预生成，不改变骰子和正式剧情。

配置中仅将 `image.concurrency` 从 2 改为 4，其他本地配置的字节一致性已核对。测试服务已停止。现有运行中的后端需要重启才会载入新代码。
