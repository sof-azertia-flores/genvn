{
  "_comment": "genvn frontend configuration. Edit this file and restart the dev server (or rebuild) to apply. apiBase: backend origin baked into `npm run build` for a front end served from another domain (CDN); empty = same origin.",
  "devServerPort": 5180,
  "devServerHost": "127.0.0.1",
  "backend": "http://localhost:8080",
  "apiBase": "https://gennovel.missazertia.com"
}
