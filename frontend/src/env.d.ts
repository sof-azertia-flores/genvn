// Injected at build time by vite.config.ts (define). Absent in tests and in any bundler that
// does not define it, so read it through `typeof` only.
declare const __GENVN_API_BASE__: string | undefined;
