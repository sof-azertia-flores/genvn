# genvn — 下一阶段 Agent 执行提示词

你将接手一个已经能够游玩的、个人使用的生成式 TRPG 视觉小说项目。

项目目录：`/Users/thiksora/Documents/Claude/genvn`。

本阶段按一个约 5 小时的自主开发窗口安排；如果用户另行指定时长，以实际指令为准。直接检查、实现、测试并交付，不要只提交方案。合理的局部重构可以自主完成，但保留用户现有配置、密钥、故事和存档。

## 1. 本阶段要实现的体验

玩家提供故事大纲与自己的角色，系统编译世界与剧情 beat，在玩家阅读时继续准备未来内容。选择和真正的程序骰子决定哪条分支成为事实。

现在核心文字循环已经成立。本阶段的首要交付是：

> 故事框架形成时，系统就为未来的人物和地点规划素材并异步绘制；详细文本与图片并行生成；后续场景优先使用合适的已有素材；玩家能够连续游玩一段有真实背景和人物立绘的视觉小说，生成的图片保存在本地并可重复使用。

图片应尽量在玩家需要它之前准备好，而不是等玩家进入场景才开始画。仅做到“场景加载后异步请求图片”不算完成本阶段。

同时继续改善叙事连贯性、等待体验和可恢复性。完成核心管线之后，再处理本提示词中的次要工作。

## 2. 先确认代码现状，保留已经成立的行为

先读 README、IMPLEMENTATION_NOTES、NEXT_WINDOW，以及当前源码和测试。文档可能落后于代码，不要重复修复已经解决的问题。

截至本提示词准备时，源码中已能看到：

- Java / Spring Boot 后端，React / TypeScript / Vite 前端，本地 JSON 存档。
- Story Compiler、Story Bible / Spine、SceneBundle、程序骰子、状态校验与提交、续章规划。
- `SpeculativeGenerator` 使用固定线程池，通过独立的 `CompletableFuture.supplyAsync` 提交候选分支；代码默认 4 个 worker，每场景最多 6 个候选。这已经是任务并行，不应简单宣称“原来是串行”。检查实际配置与请求执行过程；如果存在串行瓶颈，消除它。
- `SessionService` 会复用已经完成的分支，也会等待同一个尚在生成的分支，避免普通情况下重复生成。
- 独立 roll API 会先产生并保存真实骰子，再生成后续场景。pending roll 在失败重试、刷新、重启后应保持；已掷骰时不能通过改选或重试重掷。
- choice 请求携带场景 ID 和状态版本，旧页面冲突返回 409；异步分支使用隔离快照；删除会话后后台保存不能复活它。
- 文本 provider 已使用 SSE 接收并重组完整 JSON，以处理长请求和网关超时。这不等于前端已经能逐句流式阅读。
- 背景、立绘仍主要是渐变与字符占位，虽然模型已经输出 `backgroundPrompt` 和 `visualDescription`。

先跑必要的基线检查，记录实际结果。保留当前已经存在的修复；不要根据旧 NEXT_WINDOW 中“没有前端测试”“从未调用真实模型”等描述推断现状。

优先查看这些入口：`story/StoryCompiler`、`story/ArcContinuationService`、`narrative/SceneGenerator`、`speculation/SpeculativeGenerator`、`speculation/BranchCache`、`game/SessionService`、`prompt/Prompts`、前端场景显示与 `visual.ts`。

## 3. 核心设计：素材比正文准备得更远

保留两个不同的提前准备范围：

- **正文：** 默认仍预生成下一层 choice 的后续场景，包括检定成功与失败。
- **素材：** 在 Story Bible / Spine / Beat 框架阶段规划整个当前 arc 的主要人物、地点与统一画风；优先生成开场和接下来约 2–3 个 beat 可能使用的图片，然后随剧情推进补充。

规划范围可以大于立即付费生成的范围。不要为每个假设分支重复画完整的一套背景和人物。

建议流程如下，具体类名可根据现有代码调整：

```text
故事大纲 + 角色卡
        ↓
经校验的 Story Bible / Story Spine + 轻量视觉素材计划
        ├── 开场详细文本 → 校验 → 可开始游玩
        ├── 开场背景 / 主要人物基准立绘 → 本地素材库
        └── 后续 beat 的背景 / 立绘 → 有预算的后台队列

玩家阅读当前场景
        ├── 下一层各 choice / outcome 的详细文本并行预生成
        └── 后续 beat 素材继续生成、下载、保存

详细文本生成时读取相关素材目录
        ↓
选择语义适配的 assetId → Runtime 校验 → 前端提前加载本地图片

续章 outline 规划就绪
        ├── 后续叙事准备
        └── 续章素材规划与提前生成
```

### 3.1 提前规划素材

新增一个轻量的 `VisualAssetPlan` 或同等结构，至少表达：

- 稳定的角色 / 地点身份，以及整个故事的画风约定。
- 素材类型：场景背景、人物基准立绘、必要的表情 / 状态变体。
- 稳定 assetId、外观版本、地点时间 / 状态变体、对应 beat 与优先级。
- 可复现的图片提示词、必要参考图、依赖关系。
- 适用条件与复用范围：故事内通用，或仅适用于某个结果 / 分支。

可以让 Story Compiler 同时输出少量结构化视觉描述，也可以从已经验证的 Bible / Spine 派生计划。避免再插入一个庞大的串行 LLM 阶段，让开场等待明显变长。视觉计划出错时，应能使用已经验证的角色 / 地点描述生成最小计划或退回占位，不要因此反复重编整个故事。

最初一批优先覆盖开场背景、即将出场的主要 NPC、随后几个 beat 的常用地点。给出可配置的小规模默认值，例如 3 个地点、2–3 个主要人物的基准立绘，再按余量补常用表情；不要为了凑数量凭空新增人物或地点。

已知人物和地点的图片任务，应在框架就绪时完成规划并入队，有容量和预算时立即发出，不以详细正文完成作为启动条件。只等待确实必要的信息依赖：同一人物的表情编辑可以依赖基准图完成；其他人物、背景和正文仍应并行。

续章也要在 outline 规划后提前准备素材，而不是等玩家进入新章节才启动。候选续章被替换后，旧素材计划不能强迫新剧情采用它。

### 3.2 后续文本优先复用素材

给 Scene Generator 提供一个精简、相关的素材 manifest，包括资产 ID、所属人物 / 地点、视觉描述、适用条件和准备状态；不把整个图片库或图片 base64 塞入每次文本请求。

SceneBundle 通过明确的 assetId 或可解析的语义引用使用图片。Runtime 校验引用是否合法、是否属于正确故事和当前状态。LLM 不负责决定磁盘路径或构造文件 URL。

复用规则：

1. 优先使用与人物身份、地点、时间、服装及当前剧情事实匹配的已有素材。
2. 如果匹配素材正在生成，引用它并暂用占位图；不要改用另一人物或错误状态的图片。
3. 如果确实需要未规划的新场景 / 外观，允许继续写合理剧情，提交结构化素材需求并异步补图。
4. 不得为了使用现成图片，强迫玩家去某个地点、删除关键情节、把失败改写成成功，或篡改作者设定。

让模型主动保持可复用的地点与人物外观描述，避免每个 scene 都重新发明房间布局和衣服。图片就绪速度不应决定剧情成败。

### 3.3 素材不是剧情事实

提前画出的房间，不等于玩家已经到访；提前画出的 NPC，不等于玩家已经认识他；预生成的血迹、火灾或受伤立绘，也不等于这些事件发生过。

- 通用背景和基准立绘可以被多个候选分支复用，分支被丢弃时不必连同它们一起删除。
- 带有特定后果的变体必须有明确的适用条件；不能把“成功拿到物品”的画面显示在失败分支。
- 通用背景尽量不包含尚未确定的人物动作、事件结果或剧透。
- 面向玩家的日志和素材展示只呈现已到达、已获知的内容。开发 Inspector 可以显式显示预测内容，但必须标清。
- 图片任务完成只更新素材状态，不能推进 `GameState.stateVersion`、提交剧情、改变骰子或覆盖新场景。素材更新应有独立版本或事件身份。

## 4. 使用真正的图片模型，并保持外观一致

实现一个小而明确的 `ImageAssetProvider` 接口和至少一个真实 provider adapter，接入 GPT Image 或同类具备图片输出能力的模型。同时保留禁用模式与离线测试 provider。

图片调用必须属于应用本身可重复运行的后端管线。开发 Agent 手工调用一次画图工具并把几张图复制进仓库，不能代替这个功能。

图片服务独立配置 enabled、provider、base URL、API key、model、尺寸、质量、输出格式、超时、并发与预算。延续现有配置文件体验，更新无密钥 example；不要覆盖用户已有的文本 provider 配置。

不要假设一个兼容 `/chat/completions` 的地址就支持图片接口。按所选 provider 的实际文档实现能力，保留用户可配置的模型 ID，不把某个型号写死在业务层。OpenAI 的 Image API 区分生成和编辑；透明背景支持也取决于模型，应按实际能力选择参数和回退方式。[官方图片生成说明](https://developers.openai.com/api/docs/guides/image-generation)

人物资产要求：

- 在视觉计划中固定发型、年龄感、服装、配色、构图与故事画风。
- 每个主要人物先生成一张基准立绘；provider 支持参考图 / 编辑时，以它为基础生成少量表情变体，避免每次从零生成不同的人。
- 第一版优先基准图与少量常用表情，不铺开几十种姿态和服装组合。
- 统一角色的取景范围、比例、锚点。优先适合叠在背景上的立绘；支持真实透明背景时使用，不支持时采用明确的构图回退，不把白底图片谎称为透明素材。
- 图片一致性需要实际查看样例。仅凭相同提示词不能声称人物已经完全一致。

背景优先生成适合视觉小说舞台的横向图，给人物和对白留出位置；人物优先竖向构图。生成尺寸必须是当前 provider 支持的值，前端布局负责适配。

## 5. 并发调度：证明实际重叠，控制排队和重复请求

不要只增加几个 `async` 方法就宣布完成。

### 5.1 正文预推演

- 检查从任务提交、HTTP client 到 provider 请求的整个路径，确认独立分支可以同时运行。
- 保留快照隔离与唯一 Canon 提交。不要为了并行删除会话一致性保护。
- 保留正在生成分支的复用；玩家选中后，应提升对应任务的队列优先级，避免排在已经无用的候选之后。
- 检查当前等待在途分支时长期持有 session 锁的问题；网络等待不能让轻量状态查询和素材状态读取长时间卡住。需要调整时，以捕获版本、等待、重新校验后提交等方式维持正确性，并验证并发双击与旧页面冲突。
- 检查超时回退是否与原请求同时继续消耗资源。对排队任务、已发 HTTP 请求和迟到结果分别处理，不让一次选择触发无界重复生成。

### 5.2 图文并行

- 文字与图片有独立的队列 / 并发额度，图片慢时不能占满正文 worker。
- 如果共用一个受限 provider，明确总请求上限并给玩家正在等待的正文保留容量。
- 图片内部也应允许有限并行，独立背景和不同人物可同时生成；必要依赖形成小型任务关系即可，不引入通用工作流平台。
- 同一素材同时被多个 beat / 分支需要时，只发一次请求，其他调用订阅同一结果。
- 图片的优先级：当前缺失素材、即将到达的 beat、较远的通用素材。玩家改变方向后更新排队优先级。

### 5.3 有限资源与失败处理

采用可配置的有界队列、文字 / 图片并发上限和图片调用预算。给出保守默认值，例如图片并发 2、首批自动生成预算 12 次尝试、每个 arc 总预算 24 次尝试；这些是默认控制参数，允许用户调整，不是要求必须画满。

预算必须计入失败重试和表情编辑；每个请求默认一张图，多图请求应按张数折算。重复加载、刷新、轮询不能重新触发整批生成或重置预算。429 / 超时采用有上限的退避；缺少权限、无效参数等明确错误不要无限重试。

区分“本地停止等待”和“provider 已停止计算”。尽可能取消已失效的 HTTP 请求，但不要保证取消后一定不计费。达到预算后保留可玩性，并在设置 / Inspector 中说明暂停原因。

## 6. 本地素材库必须真正可恢复

在现有 data-dir 下保存图片二进制和轻量 metadata / manifest，例如 `assets/`。继续使用本地文件，不引入数据库、Redis 或对象存储。

缓存身份应包含故事范围、稳定角色 / 地点身份、外观或环境版本、画风、生成提示词、参考图版本及影响输出的模型参数。仅按人物名字或 locationId 缓存会串图；把每个 sceneId 都加入通用素材键则会破坏复用。合理区分素材逻辑身份与生成版本。

要求：

- 任务有明确的 planned / queued / generating / ready / failed 等状态，能持久化必要信息。
- 图片写入临时文件，验证内容与尺寸后原子发布；只有可读取的完整文件才能标为 ready。
- 后端完成 provider 结果解析和本地保存；前端访问本地资产接口，不依赖会过期的远端生成 URL。
- 素材接口按受控 ID 查找，不能让模型输出或客户端参数任意读取文件系统。密钥不进入前端响应、日志、测试快照和 example。
- 重启后已完成素材直接复用；未完成任务按有限策略恢复，不能把未知结果的付费请求无限重新发送。
- 文件缺失或损坏时转入可恢复状态，显示占位并按预算修复，避免假 ready 或循环请求。
- 图片版本变化不能覆盖旧存档所引用的图；旧存档没有图片字段时仍能加载，可惰性建立素材计划。
- 删除会话后，迟到任务不能重新创建会话或无限补图。共享素材清理可以延期，优先不误删仍被存档引用的文件。

## 7. 前端把图片提前加载好，并允许正文先走

接入真实背景和人物立绘，保留现有占位回退。

- 文本已经验证完成时即可阅读与选择，不等待整批图片。开场尤其不能等待整个素材库完成。
- 首屏文本与图片状态分别显示。玩家看到“正在准备画面”等简洁说明即可，provider / worker 细节放在 Inspector。
- 图片尚未完成时使用稳定占位；失败时不弹出阻断整个游戏的错误页。
- 图片完成后，前端加载 / 解码再替换，避免闪白与布局跳动。素材更新不能重置对白行号、重播骰子动画或再次提交选择。
- 对当前和少量即将使用的素材做浏览器预加载；不要一次下载整个未来章节素材库。
- 切场景、切存档和删除会话后，拒绝旧请求更新当前画面。核对 session、scene 与 assetId / version。
- 可用 SSE、轮询或现有机制通知图片状态；选择最简单可靠的方案。不要把后台图片等待绑到已经长期等待文字的会话接口上。

Inspector 增加足以诊断管线的信息：素材计划所属 beat、队列 / 在途 / 已完成数量、并发上限、尝试次数与预算、复用次数、失败原因，以及 plannedAt / startedAt / readyAt / firstNeededAt。用实际时间说明图片在何时启动、是否在需要前完成，不能只显示一个虚构的进度百分比。

## 8. P0 完成后的原计划延续

按照剩余时间选择，不能挤掉提前素材生成和完整游玩验证：

1. **叙事质量。** 用若干不同故事连续游玩多个场景，检查开场是否过早 completeBeat、失败是否被偷偷抹掉、人物和线索是否稳定。模型不得替玩家决定重要行为、台词和情绪；仍由程序掷骰、校验状态并提交选中分支。
2. **等待体验。** 准确区分编译世界、生成正文、生成素材。若实现前端逐段正文流，必须标记暂定内容，完整 schema 校验通过前禁用 choices / Canon 提交；repair 和失败时正确处理旧片段。没有时间则保留完整场景校验后的显示方式。
3. **读档体验。** 合理恢复当前场景的素材准备和一层分支预推演，重复 load 不能重复创建相同任务。
4. **玩家日志。** 展示已读对白、已认识的人物、已知地点、开放线索；不要把 Story Bible 中全部秘密当成玩家知识。

本阶段不扩展账户 / SaaS、多人、桌面套壳、完整战斗规则、地图编辑器、语音 / 视频、向量数据库或通用 Agent 平台。两层以上正文预推演暂缓；先让一层文字和更早的素材准备在预算内稳定工作。

## 9. 必须留下的验证证据

测试服务使用隔离 data-dir 和假凭据；不要拿用户存档做破坏性测试。新增测试集中在跨模块行为与并发正确性，不堆砌复述实现的断言。

至少覆盖以下场景：

| 场景 | 验收要求 |
| --- | --- |
| 正文分支并发 | 用可控的本地 HTTP provider / 并发门闩，证明两个独立请求在第一个完成前都已进入服务端；同时证明未超过配置上限。仅检查线程池存在不算。 |
| 图文重叠 | 图片请求被测试门闩挂起时，详细文本请求仍会进入并完成；文字、选择与真实骰子不等待该图片释放。 |
| 素材提前 | 框架就绪即规划并入队；在并发和预算允许的受控配置下，证明开场详细文本尚在生成时后续 beat 的图片请求已进入 provider。再构造有足够阅读提前量的流程，证明到达后续 beat 时能直接命中本地成图。 |
| 同素材并发复用 | 多个分支请求同一个背景 / 人物，只产生一个图片任务；后续再次使用无新付费请求。 |
| 完成顺序变化 | 图片先到、文字先到、图片迟到及失败都能继续游玩；素材更新不重置对白、不重复掷骰、不增加剧情版本。 |
| 分支正确性 | 检定失败不使用成功专属资产；未选分支不修改 Canon；被弃用任务的迟到结果不能改变当前画面。 |
| 已有骰子与缓存行为 | 等待在途候选不普通重复生成；场景失败重试复用 pending roll；刷新 / 重启也保持；过期 choice 仍返回冲突。 |
| 重启和本地复用 | 生成图片、保存、关闭、重新启动再读档，已完成图仍可用且不重新调用 provider；损坏 / 缺失图片和旧存档有回退。 |
| 调度和额度 | 队列满、429、超时、预算耗尽和取消不会无界重试，也不会让前台正文永久饥饿；重启不重置已消费的自动生成额度。 |
| 前端竞争 | 快速选择、切存档、返回菜单、图片请求失败时无串图、无旧响应覆盖、无永远卡住的加载状态。 |

运行现有后端回归、前端检查 / 测试及生产构建，再做浏览器实际游玩验证。记录真实执行的命令、结果和覆盖限制。

如果已经配置了可用图片 provider，在明确预算内完成少量真实联调，至少查看一张背景、一张人物基准图，并验证它们来自应用管线、落盘且能再次加载。若缺少配置、权限或额度，继续完成 adapter、离线管线和模拟 HTTP 验证，留下具体接入步骤；报告中明确“真实图片 API 尚未验证”，不能把占位图或测试夹具当作真实接入成功。

不要承诺任何网速、模型或玩家阅读速度下都能提前完成。交付可观察、可调节的提前准备机制，以及提前量不足时仍能游玩的回退。

## 10. 执行与交付要求

先快速确认基线，再尽早串通这一条实际链路：

> beat 素材计划 → 与正文同时发起图片任务 → 本地保存 → SceneBundle 引用 → 前端显示 → 后续场景复用 → 重启仍可读取。

这条链路跑通后再完善优先级、失败恢复、表情一致性与测试。需要多人并行时按文件 / 模块边界分工，先对齐 schema，避免同时重写 SessionService 和前端协议。

更新 README、无密钥配置示例、IMPLEMENTATION_NOTES 和 NEXT_WINDOW，使其反映实际代码。不要覆盖 live 配置，不要自动部署或公开服务。

最终交付必须包括：

- 已实现的玩家可见行为，以及如何启动 / 配置图片模型。
- 现有预推演是否端到端并行、发现了什么实际瓶颈、如何验证修复。
- 图文任务实际重叠及素材提前完成的证据；本地图片位置和一次跨场景复用 / 重启读取的示例。
- 并发、提前 beat 数、预算、失败回退的默认值与调整方法。
- 真实 provider 验证、模拟 HTTP 验证和未验证部分分别列明；不虚构时延、价格和图片效果。
- 测试结果、剩余缺陷和下一窗口最值得做的少量工作。

全程保持两条原则：**LLM 写故事，Runtime 决定事实；素材提前准备，游玩不被图片阻塞。**
